- Add inline world creation form on world list page (PR #3)
  - Replaced separate "Create World" navigation button with an inline form card on the list page
    - `world_list.html`: removed "Create World" button from page header
    - `world_list.html`: added "Create World" card with inline POST form and validation feedback
    - `world_list.html`: updated empty-state message to reference inline form instead of separate link
    - `world_list.html`: renamed card header from "Quick Create World" to "Create World" (removed "Quick")
    - `base.html`: removed "Create World" navigation link
  - Extended `WorldListView` to handle inline world creation POST requests
    - `views.py`: added `FormMixin` mixin to `WorldListView`
    - `views.py`: added `get_form_class()` using `modelform_factory(World, fields=['name'])`
    - `views.py`: added `post()` method with form validation and re-render on error
    - `views.py`: added `form_valid()` to save the new world
    - `views.py`: added `success_url = reverse_lazy('helloworld:world_list')`
    - `views.py`: added `modelform_factory` and `FormMixin` imports
  - Added tests for inline world creation
    - `helloworld/tests.py`: test for valid POST creates world and redirects
    - `helloworld/tests.py`: test for invalid POST (duplicate name) re-renders with errors
