- Add greeting animation when sending hello to a world (PR #5)
  - Added CSS keyframe animations `floatUp` and `countPop` in `world_detail.html`
  - Added `.greeting-particle` style for floating emoji particles
  - Added `#hello-area` wrapper div around the hello form for positioning particles
  - Triggered animation on page load when `?greeted=1` query parameter is present
    - Three 👋 emoji particles float upward with staggered delays
    - Hello count element receives a scale pop animation
    - URL cleaned via `window.history.replaceState` after animation to prevent replay on refresh
  - Updated `send_hello` view to redirect to detail page with `?greeted=1` appended
    - `views.py`: added `reverse` import alongside `reverse_lazy`
    - `views.py`: `send_hello` uses `reverse(...) + '?greeted=1'` instead of `redirect(...)`
  - Added test verifying `?greeted=1` appears in redirect URL after sending hello
    - `helloworld/tests.py`: new test case added
