- Create Hello World Django app (PR #1)
  - Set up Django project with `config/` for settings and `helloworld/` for the main app
    - `config/__init__.py`, `config/asgi.py`, `config/settings.py`, `config/urls.py`, `config/wsgi.py` added
    - `helloworld/__init__.py`, `helloworld/apps.py` added
  - Added `World` model with `name` (unique CharField), `hello_count` (PositiveIntegerField), `created_at` (DateTimeField) and `send_hello()` method
    - `helloworld/models.py` added
    - `helloworld/migrations/0001_initial.py` added
  - Added class-based views `WorldListView`, `WorldDetailView`, `WorldCreateView` and function-based view `send_hello`
    - `helloworld/views.py` added
    - `helloworld/urls.py` added
  - Added Bootstrap 5 templates extending a shared base
    - `helloworld/templates/helloworld/base.html` added
    - `helloworld/templates/helloworld/world_list.html` added
    - `helloworld/templates/helloworld/world_detail.html` added
    - `helloworld/templates/helloworld/world_form.html` added
  - Registered `World` in Django admin
    - `helloworld/admin.py` added
  - Added GitHub Actions CI workflow for Python 3.10, 3.11, 3.12
    - `.github/workflows/ci.yml` added
    - CI `GITHUB_TOKEN` permissions restricted to `contents: read`
  - Added comprehensive test suite for models and views
    - `helloworld/tests.py` added
  - Added project metadata and tooling files
    - `requirements.txt` added (`Django>=4.2`)
    - `manage.py` added
    - `LICENSE` (MIT) added
    - `.gitignore` added
    - `README.md` expanded with full documentation
    - `.github/copilot-instructions.md` added
